:- module(oe_plot_vis,[]).

:- use_module(library(openease)).

%oe:result_set_show(QueryID,ResultSet) :-
%    data_vis(linechart(QueryID), [
%        title: 'Some plot',
%        data: [[a,b,c],[10,30,22]]
%    ]).

