:- module(oe_plot_vis,[]).

:- use_module(library(openease)).

%oe:result_set_show(ResultSet) :-
%	atomic_list_concat(
%		['plot','testing'],'_',ID),
%    data_vis(linechart(ID), [
%        title: 'Some plot',
%        data: [[a,b,c],[10,30,22]]
%    ]).

